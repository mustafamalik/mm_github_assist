# MM GitHub Assistant Token Configuration (Optional if using `gh` CLI)
# Scopes required: repo (or fine-grained: Issues: R/W, Pull requests: R/W, Contents: R/W)
GITHUB_TOKEN=github_pat_your_personal_access_token_here
BASE_BRANCH=main
HITL_MODE=cautious
